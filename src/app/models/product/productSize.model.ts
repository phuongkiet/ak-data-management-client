export interface ProductSizeDto {
  id: number;
  wide: number;
  length: number;
  autoSized: string;
}

export interface AddSizeDto {
  wide: number;
  length: number;
  autoSized: string;
}

export interface UpdateSizeDto {
  wide: number;
  length: number;
  autoSized: string;
}


